import React from 'react'

function NavBar() {
    return (
        <div>
            <nav>
                <a href="#">Home</a>
                <a href="#">Contact Us</a>
                <a href="#">About</a>
            </nav>
        </div>
    )
}

export default NavBar
